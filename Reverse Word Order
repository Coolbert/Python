Write a program that asks the user for a long string containing multiple words. 
Print back to the user the same string, except with the words in backwards order.

def reverse(word):
    b = word.split()
    c = b[::-1]
    return c

a = input(("Input some words: "))
print (reverse(a))
