Create a program that asks the user for a number and then prints out a list of all the divisors of that number.

b = []
num = int(input("Give number: "))
divisors = list(range(1,int(num/2)))
for a in divisors:
    if num % a == 0:
        b.append(a)

print(b)
